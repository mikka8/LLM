# 🧪 UI-тесты на Cypress для AI Translator & Critic

Набор E2E UI-тестов с мокированием API через `cy.intercept()` для приложения перевода текстов с помощью LLM.

---

## 📦 Структура проекта
```
tests/ui/cypress/e2e/
├── translator_critic.cy.js # Основной набор (3 теста, соответствует заданию)
└── tests.cy.js # Расширенный набор (7 тестов)
```
---

## 🚀 Быстрый старт

### Установка зависимостей

**1. Python зависимости:**

pip install -r requirements.txt

**2. Node.js зависимости:**

npm install

**3. Для GitHub Codespaces / Ubuntu 24.04 (обязательно):**

sudo apt update
sudo apt install -y xvfb libgtk-3-0 libnss3 libasound2t64

> **Примечание:** `xvfb` нужен для запуска Cypress в headless режиме в Codespaces.

---

## 🧪 Запуск тестов

### Вариант 1: Два терминала (рекомендуется)

**Терминал 1 - Flask:**

python src/app.py

Вы должны увидеть:

 * Running on http://0.0.0.0:5000

**Терминал 2 - Cypress (headless для Codespaces):**

npm run cypress:run:linux

### Вариант 2: Интерактивный режим (локально)

npm run cypress:open


### Вариант 3: Автоматический запуск (один терминал)

Скрипт автоматически запустит Flask и Cypress одной командой:

bash RUN_TESTS.sh


Скрипт самостоятельно:
- ✅ Запустит Flask в фоновом режиме
- ✅ Дождётся инициализации сервера (3 секунды)
- ✅ Запустит Cypress тесты
- ✅ Корректно завершит Flask после тестов

### Ожидаемый результат

✔ translator_critic.cy.js    3 passing
✔ tests.cy.js                7 passing

Total: 10 passing (~7 seconds)

---

## 🧪 Что тестируется

### translator_critic.cy.js (требуется заданием)

1. **Успешный перевод и оценка** (Qwen + Claude)
   - Мокирует оба API запроса через `cy.intercept()`
   - Проверяет полный пользовательский сценарий: ввод → перевод → оценка

2. **Обработка ошибки 500**
   - Мокирует API с ошибкой 500
   - Проверяет, что приложение показывает сообщение об ошибке

3. **Smoke UI**
   - Проверяет видимость элементов формы (textarea, select, кнопки)

### tests.cy.js (расширенное покрытие)

- Отдельные тесты для перевода и оценки
- Обработка ошибок 500 и 502
- Проверка сохранения текста при смене языка
- Проверка количества опций в select

---

## 🔑 Ключевые техники

### Мокирование API (cy.intercept)

cy.intercept('POST', '**/process-ai-request', (req) => {
  if (req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct') {
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
  }
}).as('translationRequest')

cy.wait('@translationRequest')  // Синхронизация с асинхронным запросом

### Проверка результатов

cy.contains('Mocked Translation: The sun is shining.')
  .should('be.visible')

---

## 📚 Документация в коде

Каждый тест содержит подробные комментарии:
- Как работает `cy.intercept()`
- Как синхронизироваться с асинхронными операциями через `cy.wait()`
- Как проверять появление контента в UI

---

## 🛠️ npm команды

npm run cypress:open          # Интерактивное окно (локально)
npm run cypress:run:linux     # Headless с xvfb (Codespaces)
npm run cypress:run           # Headless (локально)
npm run cypress:run:headed    # С видимым браузером
npm test                      # Стандартный запуск

---

## 🐛 Типичные проблемы

### "Cannot find module 'cypress'"

npm install

### "Connection refused localhost:5000"
Убедитесь, что Flask запущен:

python src/app.py

### "Your system is missing the dependency: xvfb" (Codespaces)

sudo apt install -y xvfb libasound2t64

### Тесты проходят, но результаты не появляются
Проверьте консоль браузера (F12) на наличие JavaScript ошибок.

---

## 📊 Технические детали

- **Фреймворк:** Cypress 13.17.0
- **Конфигурация:** `cypress.config.js` (baseUrl: http://localhost:5000)
- **Мокирование:** `cy.intercept()` с wildcard pattern `**/process-ai-request`
- **Синхронизация:** `cy.wait('@alias')` вместо жёстких таймаутов

---

## ✅ Выполненные требования (из задания)

✅ **Мокирование API:**
- Mock 1: Qwen модель (`model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct'`) → 200 OK
- Mock 2: Claude модель (`model_name === 'claude-sonnet-4-5-20250929'`) → 200 OK

✅ **Основной сценарий:**
- `cy.visit('/')` → ввод "Солнце светит." → выбор "Английский" → клик "Перевести" → проверка перевода → клик "Оценить при помощи LLM-as-a-Judge" → проверка оценки

✅ **Обработка ошибок:**
- Мокирование API с 500 для обеих моделей
- Проверка сообщения об ошибке (приложение не падает)

✅ **Документация:**
- Подробные комментарии в коде
- Объяснение `cy.intercept()` и асинхронности
- Инструкции по установке и запуску в Codespaces

---

**Статус:** ✅ Готово к сдаче  
**Версия:** 1.0  
**Дата:** December 2025