/**
 * Cypress E2E Support File
 * 
 * Этот файл запускается перед каждым E2E тестом.
 * Здесь можно добавлять глобальные команды, хуки, и конфигурацию.
 */

// Отключаем остановку на необработанных исключениях
Cypress.on('uncaught:exception', (err, runnable) => {
  // Возвращаем false чтобы предотвратить падение теста
  return false
})

// Глобальная конфигурация таймаутов
beforeEach(() => {
  // Время ожидания по умолчанию - 10 секунд
  cy.visit('http://localhost:5000')
})
