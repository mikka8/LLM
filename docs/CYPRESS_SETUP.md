# Cypress E2E Тесты: Руководство по запуску

## 📋 Содержание
1. [Установка](#установка)
2. [Структура тестов](#структура-тестов)
3. [Запуск тестов](#запуск-тестов)
4. [Запуск в Codespaces](#запуск-в-codespaces)
5. [Объяснение cy.intercept()](#объяснение-cyintercept)

---

## Установка

### Предварительные требования
- **Node.js** версии 16+ ([Download](https://nodejs.org/))
- **npm** (обычно установлен с Node.js)
- **Python** с установленным приложением Flask (уже готово в проекте)

### Шаг 1: Установка зависимостей Node.js

```bash
# Перейдите в корень проекта
cd /workspaces/LLM

# Установите зависимости (в т.ч. Cypress)
npm install
```

**Что происходит:**
- npm читает `package.json`
- Загружает и устанавливает Cypress 13.6.6 в папку `node_modules/`
- Создаёт файл `package-lock.json` для фиксации версий

### Шаг 2: Убедитесь, что Python зависимости установлены

```bash
# Установите Python зависимости (если ещё не установлены)
pip install -r requirements.txt

# Добавьте pytest
pip install pytest
```

---

## Структура тестов

```
cypress/
├── e2e/
│   └── translator_critic.cy.js    # Основные E2E тесты
└── fixtures/                       # Тестовые данные (если понадобятся)

cypress.config.js                   # Конфигурация Cypress
package.json                        # Зависимости Node.js
```

### Описание файлов

| Файл | Назначение |
|------|-----------|
| `cypress/e2e/translator_critic.cy.js` | Основной файл с E2E тестами |
| `cypress.config.js` | Конфигурация: baseUrl, timeouts, браузеры |
| `package.json` | npm скрипты для запуска тестов |

---

## Запуск тестов

### Вариант 1: Cypress UI (рекомендуется для разработки)

Откроется интерактивное окно Cypress, где можно запускать тесты и смотреть их выполнение в реальном времени.

```bash
npm run cypress:open
```

**Что вы увидите:**
1. Откроется окно Cypress
2. Выберите "E2E Testing"
3. Выберите браузер (Chrome рекомендуется)
4. Нажмите на файл `translator_critic.cy.js`
5. Тесты начнут выполняться

### Вариант 2: Headless режим (для CI/CD)

Тесты запустятся в фоне без визуального окна.

```bash
# Стандартный запуск
npm run cypress:run

# С видимым окном браузера
npm run cypress:run:headed

# Запуск только одного спецификационного файла
npm run cypress:run:spec
```

### Вариант 3: Запуск одного конкретного теста

```bash
# Откроется Cypress с фильтром по тесту
npx cypress open --spec "cypress/e2e/translator_critic.cy.js"
```

---

## Запуск в Codespaces

### Предварительная настройка

1. **Убедитесь, что Flask приложение запущено:**

```bash
# В одном терминале запустите Flask сервер
python src/app.py
```

Вы должны увидеть:
```
WARNING: This is a development server. Do not use it in production deployments.
 * Running on http://0.0.0.0:5000
```

2. **Порт 5000 будет автоматически перенаправлен Codespaces.**
   Откройте его в браузере или используйте в тестах.

### Запуск тестов в Codespaces

#### Способ 1: Через VS Code Terminal

1. Откройте второе окно терминала (Ctrl + Shift + `)
2. Установите зависимости:

```bash
npm install
```

3. Запустите тесты в headless режиме:

```bash
npm run cypress:run
```

#### Способ 2: Запуск Cypress UI в Codespaces

```bash
npm run cypress:open
```

**Важно:** Cypress откроет окно в Codespaces, но оно может быть недоступно напрямую. Используйте:
- **Простой браузер VS Code** (если поддерживается)
- **Headless режим** для полной автоматизации

---

## Объяснение cy.intercept()

### Что такое cy.intercept()?

`cy.intercept()` позволяет **перехватывать и мокировать** сетевые запросы, отправляемые веб-приложением. Это критически важно для:

1. **Изоляции тестов** от реального API
2. **Контроля ответов** (успех/ошибки)
3. **Ускорения тестов** (нет реальных запросов)

### Синтаксис

```javascript
cy.intercept(method, urlPattern, handler).as('alias')
```

**Параметры:**
- `method`: HTTP метод (`'POST'`, `'GET'`, и т.д.)
- `urlPattern`: URL или regex для фильтрации
- `handler`: Функция для обработки запроса
- `alias`: Имя для ожидания с `cy.wait()`

### Пример 1: Простой mock

```javascript
cy.intercept('POST', 'https://api.mentorpiece.org/v1/process-ai-request', {
  statusCode: 200,
  body: { response: 'Mocked answer' }
}).as('apiCall')

// Ожидаем перехваченный запрос
cy.wait('@apiCall')
```

### Пример 2: Условный mock (анализ тела запроса)

```javascript
cy.intercept('POST', 'https://api.mentorpiece.org/v1/process-ai-request', (req) => {
  // Анализируем тело запроса
  if (req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct') {
    // Возвращаем ответ для модели перевода
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
    return
  }
  
  if (req.body.model_name === 'claude-sonnet-4-5-20250929') {
    // Возвращаем ответ для модели оценки
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Grade: 9/10.' }
    })
    return
  }
  
  // Для других запросов пропускаем дальше
  req.continue()
}).as('apiRequest')
```

### Пример 3: Mock ошибок

```javascript
// Mock 500 ошибки
cy.intercept('POST', '/api/endpoint', {
  statusCode: 500,
  body: { error: 'Internal Server Error' }
}).as('apiError')
```

### Как это работает в наших тестах

**Шаг 1:** Определяем intercept перед действиями

```javascript
cy.intercept('POST', API_ENDPOINT, (req) => {
  if (req.body.model_name === TRANSLATION_MODEL) {
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
  }
}).as('translationRequest')
```

**Шаг 2:** Заполняем форму и выполняем действие

```javascript
cy.get('#original_text').type('Солнце светит.')
cy.contains('button', 'Перевести').click()
```

**Шаг 3:** Ждём перехваченный запрос

```javascript
cy.wait('@translationRequest')
```

**Шаг 4:** Проверяем результат на странице

```javascript
cy.contains('Mocked Translation: The sun is shining.').should('be.visible')
```

---

## 📊 Набор тестов

### Тесты успеха (✅)

| Тест | Описание |
|------|---------|
| `Должен корректно обработать перевод текста` | Mock Qwen модель → проверяет мок-ответ |
| `Должен корректно обработать оценку качества` | Mock Claude модель → проверяет мок-ответ |
| `Полный сценарий: Перевод → Оценка` | Обе модели в одном тесте |

### Тесты обработки ошибок (🛡️)

| Тест | Описание |
|------|---------|
| `Должен обработать 500 ошибку от API` | Mock 500 → проверяет сообщение об ошибке |
| `Должен обработать ошибку при оценке` | Перевод успешен, оценка падает → проверяет обработку |

### Тесты интерфейса (🎨)

| Тест | Описание |
|------|---------|
| `Все элементы формы должны быть видимы` | Проверяет наличие и доступность элементов |
| `Должен сохранять введённый текст` | Проверяет, что текст не теряется при смене языка |

---

## 🔧 Полезные команды

```bash
# Установка Cypress вручную
npm install cypress

# Обновление Cypress
npm update cypress

# Удаление Cypress и переустановка
npm uninstall cypress
npm install cypress

# Запуск тестов в режиме отладки
npm run cypress:debug

# Очистка кэша Cypress
npx cypress cache clear
```

---

## 📝 Типичные проблемы и решения

### Проблема: "Cannot find module 'cypress'"

**Решение:**
```bash
npm install
```

### Проблема: "Connection refused: localhost:5000"

**Решение:**
Убедитесь, что Flask приложение запущено:
```bash
python src/app.py
```

### Проблема: Тесты зависают на cy.wait()

**Решение:**
1. Проверьте, что alias совпадает с именем в cy.intercept()
2. Проверьте, что запрос действительно отправляется (смотрите логи)
3. Увеличьте timeout в cypress.config.js

### Проблема: Тесты падают в headless режиме

**Решение:**
Запустите с флагом `--headed`:
```bash
npm run cypress:run:headed
```

---

## 📚 Дополнительные ресурсы

- [Официальная документация Cypress](https://docs.cypress.io/)
- [cy.intercept() документация](https://docs.cypress.io/api/commands/intercept)
- [Best Practices](https://docs.cypress.io/guides/references/best-practices)

---

## ✅ Чек-лист перед запуском

- [ ] Node.js установлен (`node --version`)
- [ ] npm установлен (`npm --version`)
- [ ] Зависимости установлены (`npm install`)
- [ ] Flask приложение запущено (`python src/app.py`)
- [ ] Порт 5000 доступен
- [ ] cypress/e2e/translator_critic.cy.js существует
- [ ] cypress.config.js существует

---

**Создано:** QA Automation Team  
**Обновлено:** December 2025
