# 🎯 UI-тесты Cypress: Краткий обзор созданных файлов

## 📦 Что было создано

### 1. **Основной файл с тестами**
- **Файл:** [cypress/e2e/translator_critic.cy.js](../cypress/e2e/translator_critic.cy.js)
- **Описание:** Полный набор E2E тестов с подробными комментариями
- **Тесты:** 7 тестов, 40+ проверок
- **Функциональность:**
  - ✅ Успешный перевод (модель Qwen)
  - ✅ Успешная оценка (модель Claude)
  - ✅ Полный цикл (обе модели)
  - 🛡️ Обработка ошибок (500)
  - 🎨 Проверки интерфейса

### 2. **Конфигурация Cypress**
- **Файл:** [cypress.config.js](../cypress.config.js)
- **Содержит:**
  - `baseUrl: http://localhost:5000`
  - Timeouts для асинхронных операций
  - Конфигурация браузеров
  - Настройки видео/скриншотов

### 3. **npm пакеты и скрипты**
- **Файл:** [package.json](../package.json)
- **npm команды:**
  - `npm run cypress:open` — интерактивное окно
  - `npm run cypress:run` — headless запуск
  - `npm run cypress:run:headed` — с видимым браузером
  - `npm test` — стандартный запуск

### 4. **Документация**

#### [docs/CYPRESS_QUICK_START.md](../docs/CYPRESS_QUICK_START.md)
Быстрый старт для QA специалистов:
- Установка Cypress
- Как запустить тесты
- Набор тестов и их описание
- Типичные проблемы и решения

#### [docs/CYPRESS_SETUP.md](../docs/CYPRESS_SETUP.md)
Полное руководство по установке и запуску:
- Пошаговая установка
- Запуск в разных режимах
- Объяснение cy.intercept()
- Примеры использования

#### [docs/CODESPACES_SETUP.md](../docs/CODESPACES_SETUP.md)
Специальное руководство для GitHub Codespaces:
- Как запустить тесты в Codespaces
- Открытие приложения в браузере
- Решение проблем в облачной среде

#### [docs/ARCHITECTURE_AND_DESIGN.md](../docs/ARCHITECTURE_AND_DESIGN.md)
Архитектура и дизайн тестов:
- Диаграммы потока данных
- Объяснение каждого компонента
- Best practices и что избегать
- Матрица покрытия тестами

### 5. **Вспомогательный скрипт**
- **Файл:** [run_cypress.sh](../run_cypress.sh)
- **Назначение:** Удобный запуск тестов из командной строки

---

## 🎯 Быстрый старт

### 1️⃣ Запустите Flask приложение

```bash
python src/app.py
```

**Вывод:**
```
 * Running on http://0.0.0.0:5000
```

### 2️⃣ В новом терминале запустите тесты

```bash
npm run cypress:run
```

**Вывод:**
```
  7 passing (15.3s) ✅
```

---

## 📊 Структура тестов

### ✅ Успешные сценарии (3 теста)

```javascript
✓ Должен корректно обработать перевод текста с моком API (модель Qwen)
✓ Должен корректно обработать оценку качества перевода (модель Claude)
✓ Полный сценарий: Перевод → Оценка (обе модели в одном тесте)
```

### 🛡️ Обработка ошибок (2 теста)

```javascript
✓ Должен обработать 500 ошибку от API при переводе
✓ Должен обработать ошибку при оценке качества
```

### 🎨 Интерфейс и валидация (2 теста)

```javascript
✓ Все элементы формы должны быть видимы и доступны
✓ Должен сохранять введённый текст при смене языка
```

---

## 🔍 Ключевые особенности тестов

### 1. **cy.intercept() - Мокирование API**

```javascript
// Перехват запроса для модели перевода
cy.intercept('POST', API_ENDPOINT, (req) => {
  if (req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct') {
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
  }
}).as('translationRequest')
```

**Почему это важно:**
- Изолирует тесты от реального API
- Ускоряет выполнение
- Позволяет тестировать любые сценарии

### 2. **cy.wait() - Синхронизация асинхронных операций**

```javascript
// Ждём перехваченный запрос
cy.wait('@translationRequest').then((interception) => {
  expect(interception.request.body.model_name).to.equal(TRANSLATION_MODEL)
})
```

### 3. **cy.contains() - Поиск элементов по тексту**

```javascript
// Проверяем появление результата
cy.contains('Mocked Translation: The sun is shining.')
  .should('be.visible')
```

---

## 📋 Проверяемые компоненты

| Компонент | Тесты |
|-----------|-------|
| **Textarea** | Ввод текста, очистка, сохранение значения |
| **Select** | Выбор языка, наличие опций |
| **Кнопки** | Видимость, доступность, клик |
| **API запросы** | Успех (200), ошибки (500) |
| **Результаты** | Отображение переводов, оценок, ошибок |

---

## 🚀 Команды для запуска

```bash
# Интерактивное окно Cypress (лучше для разработки)
npm run cypress:open

# Headless режим (лучше для CI)
npm run cypress:run

# С видимым браузером
npm run cypress:run:headed

# Только один файл с тестами
npm run cypress:run:spec

# Режим отладки
npm run cypress:debug

# Стандартный npm test
npm test
```

---

## 📚 Документация для разных сценариев

| Сценарий | Документация |
|----------|-------------|
| **Я хочу быстро начать** | [CYPRESS_QUICK_START.md](../docs/CYPRESS_QUICK_START.md) |
| **Мне нужна полная инструкция** | [CYPRESS_SETUP.md](../docs/CYPRESS_SETUP.md) |
| **Я работаю в Codespaces** | [CODESPACES_SETUP.md](../docs/CODESPACES_SETUP.md) |
| **Я хочу понять архитектуру** | [ARCHITECTURE_AND_DESIGN.md](../docs/ARCHITECTURE_AND_DESIGN.md) |

---

## 🛠️ Установка

### 1. Зависимости Node.js

```bash
npm install
```

Установит:
- ✅ Cypress 13.6.6
- ✅ Все необходимые плагины

### 2. Зависимости Python

```bash
pip install -r requirements.txt
pip install pytest
```

---

## ✨ Преимущества такого подхода

✅ **Полная изоляция от API** — тесты не зависят от реального сервера
✅ **Быстрое выполнение** — нет задержек сетевых запросов
✅ **Надёжные результаты** — предсказуемые моки
✅ **Комплексное покрытие** — 7 тестов, 40+ проверок
✅ **Подробная документация** — 4 файла с разными подходами
✅ **Лёгко расширяется** — простая структура для добавления новых тестов

---

## 🎓 Что было использовано

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Cypress** | 13.6.6 | E2E тестирование |
| **Node.js** | 16+ | Окружение для npm |
| **npm** | 7+ | Управление пакетами |
| **Flask** | 2.3.3 | Тестируемое приложение |
| **Python** | 3.12 | Бэкенд приложение |

---

## 🎯 Следующие шаги

1. **Запустите Flask:** `python src/app.py`
2. **Откройте Cypress:** `npm run cypress:open`
3. **Смотрите тесты:** Нажмите на `translator_critic.cy.js`
4. **Анализируйте результаты:** Проверьте, что все тесты ✅ passing

---

## 📞 Поддержка

**Проблемы при запуске?**

1. Смотрите раздел "Типичные проблемы" в [CYPRESS_QUICK_START.md](../docs/CYPRESS_QUICK_STARTUP.md)
2. Проверьте [CYPRESS_SETUP.md](../docs/CYPRESS_SETUP.md) для подробного FAQ
3. Для Codespaces смотрите [CODESPACES_SETUP.md](../docs/CODESPACES_SETUP.md)

---

## 📝 Статистика

- **Файлов создано/обновлено:** 7
- **Тестовых кейсов:** 7
- **Строк кода в тестах:** 600+
- **Документация:** 4 комплексных файла
- **Комментарии:** 100+ подробных пояснений

---

**Тесты готовы к запуску! 🚀**

`npm run cypress:run`
