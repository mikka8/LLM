# 🌐 Запуск Cypress тестов в GitHub Codespaces

## 📋 Краткое описание

Этот документ объясняет, как запустить Cypress E2E тесты в среде GitHub Codespaces.

---

## 🚀 Шаг за шагом

### Шаг 1: Откройте проект в Codespaces

1. На странице репозитория нажмите **Code** → **Codespaces** → **Create codespace on main**
2. Дождитесь загрузки окружения (обычно 2-3 минуты)
3. Откроется VS Code в браузере

### Шаг 2: Откройте два терминала

1. **Первый терминал** (Flask приложение):
   - Нажмите Ctrl + ` (или Terminal → New Terminal)
   - Запустите: `python src/app.py`
   
2. **Второй терминал** (Cypress тесты):
   - Нажмите Ctrl + Shift + ` (создать новый терминал)
   - Зависит от вашей ОС

**Вы должны увидеть в первом терминале:**
```
 * Running on http://0.0.0.0:5000
 * Debug mode: on
```

### Шаг 3: Установите зависимости (если нужно)

Во втором терминале:

```bash
npm install
```

Если npm не найден, нужно установить Node.js:
```bash
# Для Ubuntu в Codespaces
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Шаг 4: Запустите Cypress

#### Вариант 1: Headless режим (рекомендуется для Codespaces)

```bash
npm run cypress:run
```

**Вывод:**
```
  Running:  cypress/e2e/translator_critic.cy.js

  AI Translator & Critic - E2E Tests Suite
    Успешный сценарий: Перевод и Оценка
      ✓ Должен корректно обработать перевод текста (2.5s)
      ✓ Должен корректно обработать оценку качества (1.8s)
      ✓ Полный сценарий: Перевод → Оценка (4.2s)
    Сценарии обработки ошибок
      ✓ Должен обработать 500 ошибку от API (1.2s)
      ✓ Должен обработать ошибку при оценке (2.0s)
    Проверки интерфейса и валидация
      ✓ Все элементы формы должны быть видимы (1.5s)
      ✓ Должен сохранять введённый текст (2.1s)

  7 passing (15.3s)
```

#### Вариант 2: С видимым браузером

```bash
npm run cypress:run:headed
```

Браузер откроется и будут видны все действия тестов.

#### Вариант 3: Интерактивный режим (если возможно)

```bash
npm run cypress:open
```

> **Примечание:** В Codespaces интерактивное окно может не отобразиться. Используйте headless режим.

---

## 🔗 Открытие приложения в браузере

### Метод 1: Forwarded Ports (автоматический)

1. В терминале, где запущен Flask, нажмите на ссылку `http://localhost:5000`
2. VS Code автоматически откроет приложение в браузере хоста

### Метод 2: Вручную через Ports

1. Нажмите на иконку **Ports** в левой панели VS Code
2. Найдите порт **5000**
3. Нажмите на ссылку рядом с `5000`

### Метод 3: Скопировать URL

1. В левой панели найдите **PORTS**
2. Кликните правой кнопкой на порт 5000
3. Выберите **Copy Forwarded Port**
4. Откройте скопированный URL в браузере

**Приложение должно открыться:**
```
http://localhost:5000 (или similar URL)
```

---

## 🔍 Проверка статуса

### Проверить Flask приложение

```bash
# В браузере откройте
http://localhost:5000

# Вы должны увидеть форму с полями:
# - Исходный текст (textarea)
# - Целевой язык (select)
# - Кнопки "Перевести" и "Оценить"
```

### Проверить Cypress

```bash
# В терминале
npx cypress --version

# Вывод: cypress/13.6.6
```

### Проверить npm пакеты

```bash
# В терминале
npm list cypress

# Вывод: cypress@13.6.6
```

---

## 📊 Результаты тестов

### Успешное прохождение

```
  7 passing (15.3s) ✅
```

### Если тесты падают

**Проверьте:**

1. **Flask запущен?**
   ```bash
   curl http://localhost:5000
   ```
   Должен вернуть HTML

2. **Порт 5000 доступен?**
   ```bash
   lsof -i :5000
   ```
   Должен показать процесс Python

3. **Cypress установлен?**
   ```bash
   npm list cypress
   ```
   Должна быть версия 13.6.6

---

## 🛑 Типичные проблемы в Codespaces

### ❌ Проблема: "npm: command not found"

**✅ Решение:**
Node.js не установлен. Установите:

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs npm
```

Проверьте:
```bash
node --version
npm --version
```

### ❌ Проблема: "Connection refused localhost:5000"

**✅ Решение:**
Flask приложение не запущено. В первом терминале:

```bash
python src/app.py
```

### ❌ Проблема: "Cannot find module 'cypress'"

**✅ Решение:**
Зависимости не установлены:

```bash
npm install
```

### ❌ Проблема: Тесты зависают

**✅ Решение:**

1. Проверьте логи Flask (первый терминал)
2. Посмотрите логи Cypress (второй терминал)
3. Перезапустите Flask:
   ```bash
   # Ctrl + C в первом терминале
   # Затем
   python src/app.py
   ```

### ❌ Проблема: "Timeout: cy.wait() waiting on XHR"

**✅ Решение:**
Intercept не работает правильно. Проверьте:

1. API endpoint правильный: `https://api.mentorpiece.org/v1/process-ai-request`
2. Model names точно совпадают с проверкой
3. Нет других перехватов, которые блокируют запрос

---

## 📈 Рекомендуемый workflow

### Для разработки

```bash
# Терминал 1: Flask
python src/app.py

# Терминал 2: Cypress (continuous)
npm run cypress:run -- --watch

# Или запуск отдельных тестов
npx cypress open
```

### Для CI/CD

```bash
# Однократный запуск всех тестов
npm run cypress:run

# Запуск специфического теста
npx cypress run --spec "cypress/e2e/translator_critic.cy.js"
```

---

## 📚 Полезные команды

```bash
# Проверить, что Cypress установлен
npx cypress --version

# Открыть Cypress UI
npm run cypress:open

# Запустить все тесты
npm run cypress:run

# Запустить с видимым браузером
npm run cypress:run:headed

# Запустить в режиме отладки
npm run cypress:debug

# Очистить кэш Cypress
npx cypress cache clear

# Переустановить Cypress
npm uninstall cypress && npm install cypress
```

---

## 🔐 Переменные окружения

Создайте файл `.env` в корне проекта (если требуется):

```bash
# .env
MENTORPIECE_ENDPOINT=https://api.mentorpiece.org/v1/process-ai-request
MENTORPIECE_API_KEY=your-api-key-here
```

> **Примечание:** Для тестов с мокированием эти переменные не обязательны, так как API полностью мокируется.

---

## ✅ Финальная проверка

Перед запуском тестов убедитесь:

- [ ] Repository клонирован
- [ ] Node.js установлен (`node --version`)
- [ ] npm установлен (`npm --version`)
- [ ] Зависимости установлены (`npm install`)
- [ ] Python зависимости установлены (`pip install -r requirements.txt`)
- [ ] Flask приложение запущено (`python src/app.py`)
- [ ] Порт 5000 доступен (`curl http://localhost:5000`)

---

## 🎓 Дополнительные ресурсы

- [Cypress Documentation](https://docs.cypress.io/)
- [GitHub Codespaces](https://github.com/features/codespaces)
- [Node.js Download](https://nodejs.org/)

---

## 💡 Советы для Codespaces

1. **Экономия ресурсов:** Используйте headless режим (`npm run cypress:run`)
2. **Скорость:** Кэш Codespaces будет сохранён между запусками
3. **Портфорwarding:** Автоматически работает для localhost:5000
4. **Терминалы:** Держите оба терминала открытыми для удобства

---

**Готово! Запустите тесты и наслаждайтесь! 🎉**

Следующий шаг:
```bash
npm run cypress:run
```
