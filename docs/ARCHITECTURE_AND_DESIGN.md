# 🏗️ Архитектура и Дизайн E2E Тестов

## Общий обзор

Этот документ объясняет архитектуру UI/E2E тестов для приложения "AI Translator & Critic" на основе Cypress.

---

## 📐 Архитектура системы тестирования

```
┌─────────────────────────────────────────────────────────┐
│           Cypress E2E Test Suite                        │
│         (cypress/e2e/translator_critic.cy.js)           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ describe('Success Scenarios')                   │  │
│  │  ├─ Test: Translation (Qwen Model)             │  │
│  │  ├─ Test: Evaluation (Claude Model)            │  │
│  │  └─ Test: Full Flow (Both Models)              │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ describe('Error Handling Scenarios')            │  │
│  │  ├─ Test: 500 Error on Translation             │  │
│  │  └─ Test: Error on Evaluation                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ describe('UI & Validation')                     │  │
│  │  ├─ Test: Form Elements Visibility             │  │
│  │  └─ Test: Text Persistence                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────┐
│        cy.intercept() - API Mocking Layer              │
│                                                         │
│  ┌─ Mock Translation API (Qwen)                      │
│  ├─ Mock Evaluation API (Claude)                     │
│  └─ Mock Error Responses (500)                       │
└─────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────┐
│          Flask Application (http://localhost:5000)      │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  GET /          — Загрузить страницу             │  │
│  │  POST /         — Отправить текст                │  │
│  │    → call_llm() — API запросы                    │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных при тесте

### Пример: Тест успешного перевода

```
1. Test Starts
   ↓
2. cy.intercept() sets up mock
   ├─ Method: POST
   ├─ URL: https://api.mentorpiece.org/v1/process-ai-request
   ├─ Condition: req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct'
   └─ Response: { response: 'Mocked Translation: The sun is shining.' }
   ↓
3. cy.visit('/')
   └─ Load index.html at localhost:5000
   ↓
4. User fills form
   ├─ cy.get('#original_text').type('Солнце светит.')
   └─ cy.get('#target_lang').select('English')
   ↓
5. User clicks button
   └─ cy.contains('button', 'Перевести').click()
   ↓
6. Browser sends POST request
   ├─ URL: https://api.mentorpiece.org/v1/process-ai-request
   ├─ Body: { model_name: 'Qwen/...', ... }
   ↓
7. cy.intercept() catches request
   ├─ Analyzes req.body.model_name
   ├─ Matches Qwen model
   └─ Returns mocked response immediately
   ↓
8. Flask renders template with response
   └─ Updates UI with: 'Mocked Translation: The sun is shining.'
   ↓
9. cy.wait('@translationRequest')
   └─ Waits for the intercepted request
   ↓
10. Test assertions
    └─ cy.contains('Mocked Translation: The sun is shining.')
       .should('be.visible')
   ↓
11. ✅ Test PASSED
```

---

## 🎯 Стратегия мокирования API

### Почему мокируется API?

| Причина | Объяснение |
|---------|-----------|
| **Изоляция** | Тесты не зависят от реального API |
| **Скорость** | Нет задержек сетевых запросов |
| **Надёжность** | Результаты предсказуемы |
| **Контроль** | Можем тестировать любые сценарии (успех/ошибка) |

### Три основных сценария мокирования

#### 1️⃣ Успешный перевод (200 OK)

```javascript
cy.intercept('POST', endpoint, (req) => {
  if (req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct') {
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
  }
})
```

#### 2️⃣ Успешная оценка (200 OK)

```javascript
cy.intercept('POST', endpoint, (req) => {
  if (req.body.model_name === 'claude-sonnet-4-5-20250929') {
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Grade: 9/10. Fluent and accurate.' }
    })
  }
})
```

#### 3️⃣ Ошибка сервера (500)

```javascript
cy.intercept('POST', endpoint, {
  statusCode: 500,
  body: { error: 'Internal Server Error' }
})
```

---

## 📊 Структура одного теста

```javascript
it('Должен корректно обработать перевод текста', () => {
  // ====== STEP 1: SETUP ======
  // Настройка мокирования API
  cy.intercept('POST', API_ENDPOINT, (req) => {
    if (req.body.model_name === TRANSLATION_MODEL) {
      req.reply({
        statusCode: 200,
        body: { response: 'Mocked Translation: The sun is shining.' }
      })
      return
    }
    req.continue()
  }).as('translationRequest')

  // ====== STEP 2: ACTION ======
  // Действия пользователя
  cy.get('#original_text').type('Солнце светит.')
  cy.get('#target_lang').select('English')
  cy.contains('button', 'Перевести').click()

  // ====== STEP 3: WAIT ======
  // Ожидание асинхронного запроса
  cy.wait('@translationRequest').then((interception) => {
    // Дополнительная проверка запроса
    expect(interception.request.body.model_name).to.equal(TRANSLATION_MODEL)
  })

  // ====== STEP 4: ASSERT ======
  // Проверки результатов
  cy.contains('Mocked Translation: The sun is shining.')
    .should('be.visible')
})
```

---

## 🧩 Компоненты тестирования

### 1. Cypress Selectors (поиск элементов)

```javascript
// По ID
cy.get('#original_text')

// По тексту кнопки
cy.contains('button', 'Перевести')

// По классу
cy.get('.form-control')

// Комплексные селекторы
cy.get('#target_lang option[value="English"]')
```

### 2. Assertions (проверки)

```javascript
// Видимость
.should('be.visible')

// Значение
.should('have.value', 'test')

// Текст
.should('contain', 'text')

// Существование
.should('exist')

// Доступность
.should('be.enabled')
```

### 3. Interactions (действия пользователя)

```javascript
// Ввод текста
.type('text')

// Выбор опции
.select('English')

// Клик
.click()

// Очистка
.clear()
```

---

## 🔗 Связь между уровнями тестирования

```
┌────────────────────────────────────────────────────────┐
│         Unit Tests (pytest) - tests/unit/               │
│  Тестируют функции: call_llm(), обработка ошибок       │
│  Используют: mocks для requests.post()                 │
│  Запуск: pytest -q                                     │
└────────────────────────────────────────────────────────┘
           ↓
┌────────────────────────────────────────────────────────┐
│       E2E Tests (Cypress) - cypress/e2e/                │
│  Тестируют пользовательские сценарии                   │
│  Используют: cy.intercept() для мокирования API        │
│  Запуск: npm run cypress:run                           │
└────────────────────────────────────────────────────────┘
```

---

## 📈 Матрица покрытия

| Компонент | Unit | E2E | Описание |
|-----------|------|-----|---------|
| `call_llm()` | ✅ | ✅ | Функция API запросов |
| Обработка ошибок | ✅ | ✅ | Перехват исключений |
| Загрузка окружения | ✅ | ❌ | Переменные окружения |
| Форма HTML | ❌ | ✅ | Интерфейс пользователя |
| Взаимодействие API | ✅ | ✅ | Отправка/получение данных |
| Асинхронные операции | ❌ | ✅ | Ожидание ответов |

---

## 🚀 Best Practices использованные в тестах

### ✅ Что правильно

1. **Мокирование с cy.intercept()**
   - Изолирует тесты от внешних зависимостей
   - Ускоряет выполнение

2. **Ждём с cy.wait()**
   - Синхронизирует асинхронные операции
   - Гарантирует, что действие выполнено

3. **Используем cy.contains()**
   - Ищет элементы по текстовому содержимому
   - Удобна для проверки результатов

4. **Подробные комментарии**
   - Объясняют "почему" для каждого шага
   - Помогают новым QA специалистам

5. **Логирование с console.log()**
   - Видно в консоли Cypress
   - Помогает при отладке

### ❌ Что избегаем

```javascript
// ❌ ПЛОХО: cy.wait(1000) для задержек
cy.wait(1000)

// ✅ ХОРОШО: cy.wait() для асинхронных операций
cy.wait('@apiRequest')

// ❌ ПЛОХО: Тесты зависят от реального API
cy.request('POST', 'https://api.external.com/...')

// ✅ ХОРОШО: Мокируем API
cy.intercept('POST', 'https://api.external.com/...', mock)

// ❌ ПЛОХО: Жёсткие таймауты
cy.get('#result').should('be.visible', { timeout: 30000 })

// ✅ ХОРОШО: Использовать стандартные таймауты
cy.get('#result').should('be.visible')
```

---

## 🔧 Расширение тестов

### Как добавить новый тест

```javascript
describe('New Test Suite', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  it('Описание нового теста', () => {
    // 1. Setup (мокирование)
    cy.intercept('POST', endpoint, mock).as('request')
    
    // 2. Actions (действия пользователя)
    cy.get('#element').type('text')
    
    // 3. Wait (ожидание)
    cy.wait('@request')
    
    // 4. Assert (проверки)
    cy.contains('expected text').should('be.visible')
  })
})
```

### Как добавить новый mock

```javascript
const newMock = {
  statusCode: 200,
  body: { response: 'Custom response' }
}

cy.intercept('POST', 'https://api.endpoint.com/', newMock)
```

---

## 📚 Ресурсы для обучения

| Ресурс | Назначение |
|--------|-----------|
| `cypress/e2e/translator_critic.cy.js` | Примеры всех концептов |
| `docs/CYPRESS_SETUP.md` | Полное руководство установки |
| `docs/CYPRESS_QUICK_START.md` | Быстрый старт |
| Cypress Docs | https://docs.cypress.io |

---

**Создано:** QA Automation Lead  
**Версия:** 1.0  
**Обновлено:** December 2025
