## 🌐 Настройка и Запуск Тестов в GitHub Codespaces

GitHub Codespaces (или другие облачные среды Linux) используют Headless-окружение, что требует установки виртуального дисплея (`Xvfb`) для запуска UI-тестов Cypress.

### 1. Установка Системных Зависимостей (Обязательный Шаг)

Для корректного запуска Cypress в Headless-режиме, необходимо установить зависимости браузера и виртуальный дисплей.

#### А. Установка для Debian/Ubuntu (Codespaces по умолчанию)

Выполните эту команду, чтобы установить все необходимые библиотеки для Cypress и `Xvfb`.

```bash
sudo apt update && sudo apt install -y \
  xvfb \
  libgtk-3-0 \
  libnss3 \
  libasound2 \
  libxss1 \
  tree
