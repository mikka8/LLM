# 🚀 Быстрый старт: UI Тесты Cypress

## ✅ Что уже сделано

1. ✅ **Полный набор E2E тестов** создан в `cypress/e2e/translator_critic.cy.js`
   - Тесты успешного перевода и оценки
   - Тесты обработки ошибок (500, недоступность сервиса)
   - Тесты интерфейса и валидации

2. ✅ **Cypress установлен** (`npm install`)
   - Версия: 13.6.6
   - Зависимости: 175 пакетов

3. ✅ **Конфигурация создана**
   - `cypress.config.js` с baseUrl и параметрами
   - `package.json` с npm скриптами

4. ✅ **Документация подготовлена**
   - `docs/CYPRESS_SETUP.md` — полное руководство

---

## 🎯 Как запустить тесты

### 1️⃣ Убедитесь, что Flask приложение запущено

В первом терминале:
```bash
python src/app.py
```

Вы должны увидеть:
```
 * Running on http://0.0.0.0:5000
 * Running on http://127.0.0.1:5000
```

### 2️⃣ Откройте Cypress UI (интерактивный режим)

Во втором терминале:
```bash
npm run cypress:open
```

**Что происходит:**
1. Откроется окно Cypress
2. Выберите "E2E Testing" → Chrome
3. Нажмите `translator_critic.cy.js`
4. Начнутся тесты!

### 3️⃣ Или запустите в headless режиме (фоновый)

```bash
npm run cypress:run
```

---

## 📋 Набор тестов

### ✅ Успешные сценарии

```javascript
// Тест 1: Перевод текста (модель Qwen)
Должен корректно обработать перевод текста с моком API

// Тест 2: Оценка качества (модель Claude)
Должен корректно обработать оценку качества перевода

// Тест 3: Полный цикл (обе модели)
Полный сценарий: Перевод → Оценка (обе модели в одном тесте)
```

### 🛡️ Обработка ошибок

```javascript
// Тест 4: 500 ошибка при переводе
Должен обработать 500 ошибку от API при переводе

// Тест 5: Ошибка при оценке (перевод успешен)
Должен обработать ошибку при оценке качества
```

### 🎨 Интерфейс

```javascript
// Тест 6: Наличие элементов
Все элементы формы должны быть видимы и доступны

// Тест 7: Валидация формы
Должен сохранять введённый текст при смене языка
```

---

## 🔍 Как работает мокирование (cy.intercept)

### Что происходит в тестах:

1. **Перехват запроса:**
```javascript
cy.intercept('POST', 'https://api.mentorpiece.org/v1/process-ai-request', (req) => {
  if (req.body.model_name === 'Qwen/Qwen3-VL-30B-A3B-Instruct') {
    // Возвращаем мок вместо реального API
    req.reply({
      statusCode: 200,
      body: { response: 'Mocked Translation: The sun is shining.' }
    })
  }
}).as('translationRequest')
```

2. **Заполняем форму:**
```javascript
cy.get('#original_text').type('Солнце светит.')
cy.get('#target_lang').select('English')
cy.contains('button', 'Перевести').click()
```

3. **Ждём мокированный запрос:**
```javascript
cy.wait('@translationRequest')
```

4. **Проверяем результат:**
```javascript
cy.contains('Mocked Translation: The sun is shining.').should('be.visible')
```

---

## 📊 Структура проекта

```
LLM/
├── src/
│   ├── app.py                          # Flask приложение
│   └── templates/
│       └── index.html                  # HTML шаблон
├── cypress/
│   └── e2e/
│       └── translator_critic.cy.js     # ✨ E2E тесты (обновлены)
├── docs/
│   ├── CYPRESS_SETUP.md                # 📖 Полное руководство
│   └── TESTS.md                        # Документация тестов
├── tests/
│   └── unit/
│       └── test_app.py                 # Python unit тесты
├── cypress.config.js                   # ✨ Конфигурация Cypress
├── package.json                        # ✨ npm скрипты
└── requirements.txt                    # Python зависимости
```

---

## 🛠️ Полезные npm команды

```bash
# Интерактивное окно Cypress
npm run cypress:open

# Запуск всех тестов в headless режиме
npm run cypress:run

# Запуск с видимым окном браузера
npm run cypress:run:headed

# Запуск только одного файла с тестами
npm run cypress:run:spec

# Запуск в режиме отладки
npm run cypress:debug

# Стандартный запуск (используется в CI)
npm test
```

---

## 🚨 Типичные проблемы

### ❌ Проблема: "Cannot find module 'cypress'"
**✅ Решение:**
```bash
npm install
```

### ❌ Проблема: "Connection refused localhost:5000"
**✅ Решение:**
Убедитесь, что Flask запущен:
```bash
python src/app.py
```

### ❌ Проблема: Тесты зависают
**✅ Решение:**
Проверьте:
1. Flask приложение запущено
2. Порт 5000 доступен
3. Нет ошибок в консоли Flask

### ❌ Проблема: Порт 5000 занят
**✅ Решение:**
```bash
# Найдите процесс на порту 5000
lsof -i :5000

# Завершите его (если нужно)
kill -9 <PID>
```

---

## 📚 Дополнительная информация

### Комментарии в коде

Каждый тест содержит подробные комментарии:
- **Что происходит:** Пояснение шага
- **Почему так:** Объяснение стратегии
- **Как это работает:** Детали реализации

Смотрите `cypress/e2e/translator_critic.cy.js` для полного объяснения!

### Справочник cy.intercept()

Находится в конце файла `translator_critic.cy.js`:
- Как использовать cy.intercept()
- Примеры для разных сценариев
- Лучшие практики

### Полное руководство

Смотрите `docs/CYPRESS_SETUP.md` для:
- Установки Cypress в Codespaces
- Запуска в CI/CD
- Продвинутого использования

---

## ✨ Что тестируется

| Компонент | Тесты |
|-----------|-------|
| **Textarea** | Ввод текста, сохранение значения |
| **Select** | Выбор языка, наличие опций |
| **Кнопка "Перевести"** | Клик, отправка запроса, обработка ответа |
| **Кнопка "Оценить"** | Клик, отправка запроса, обработка ответа |
| **API (мокирована)** | Успех (200), ошибка (500) |
| **Результаты** | Отображение переводов, оценок, ошибок |

---

## 🎓 Для QA специалистов

**Основные концепции E2E тестирования:**

1. **cy.intercept()** — перехват и мокирование API
2. **cy.wait()** — синхронизация с асинхронными операциями
3. **cy.contains()** — поиск элементов по тексту
4. **Selectors** — поиск элементов по id, class, xpath
5. **Assertions** — проверки видимости и значений

**Каждый из этих концептов используется в тестах!**

---

**Готово к запуску! 🚀**

Следующий шаг: `npm run cypress:open`
